DB_SERVER=localhost
DB_PORT=5432
DB_NAME=tamtdb
DB_USERNAME=tamtuser
DB_PASSWORD=TAMTTAMT

# ログ（任意・既定で logs/ に日次ローテーション）
# LOG_DIR=logs
# LOG_LEVEL=INFO
# LOG_BACKUP_COUNT=30

# 認証 Cookie / JWT 受信ログ（API_LOGIN_SPEC.md 参照。スケジュール API は Cookie のみ確認）
# COOKIE_NAME=access_token
# AUTH_LOG_JWT_CLAIMS=false
# JWT_SECRET_KEY=  （認証サービスと同一。設定時は署名検証済みクレームをログに出す）
# JWT_ALGORITHM=HS256

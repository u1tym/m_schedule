フロントエンドを修正して欲しい。

歯車クリックで、月表示の設定のところで、今、「一覧表示」「カレンダー表示」があるけど、「カレンダー表示（PC用）」を追加して欲しい。

「カレンダー表示（PC用）」の場合、「カレンダー表示」と同じように、週の始まりを選択可能とする。

また、「カレンダー表示（PC用）」を選択した場合、API(settings)では、calender-monthly-typeにpcboxを設定する。

フロントエンドの表示で、calender-monthly-typeの値が想定外の場合には、「カレンダーなし」を表示する。

